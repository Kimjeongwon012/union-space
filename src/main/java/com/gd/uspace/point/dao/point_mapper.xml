<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC	"-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"
>
<mapper namespace="com.gd.uspace.point.dao.PointDAO">

	<select id="list" resultType="com.gd.uspace.point.dto.PointDTO">
		SELECT 
		    p.user_id,
		    p.point_price,
		    p.point_list,
		    p.point_date,
		    s.space_name,
		    u.user_point
		FROM point p
		    JOIN us_group g ON p.group_no = g.group_no 
		    JOIN space s ON g.space_no = s.space_no
		    JOIN user u ON p.user_id = u.user_id;
	</select>
	
</mapper>